// import { validate } from 'class-validator';
// import Joi from 'joi';

// export default async function validationFunction(entity: string, dataValidate: object) {
//   const errors = await validate(entity, dataValidate);
//   console.log('errors', errors);
//   if (errors.length > 0) throw new Error(`Validation failed!`);
// }
